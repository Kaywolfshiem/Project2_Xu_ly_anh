1.Bài gồm 2 phần là High-Low pass với ma trận kernel và Low pass với Gaussian.

2.Xin hãy kiểm tra kĩ đường dẫn đã lưu file git này, vì trong code sẽ yêu cầu nhập nơi lưu trữ file git. 
Lưu ý chỉ nhập tới khúc nơi lưu trữ, không nhập thêm tên của file git vào. Ví dụ tải file git về ổ D thì khi nhập đường dẫn
vào chương trình thì chỉ nhập D: thay vì D:/bai2, nếu dùng dấu / không được hãy thử đổi lại dấu \ khi nhập đường dẫn.

3.Khuyến khích dùng ảnh đã gửi kèm bài, vì 1 số định dạng ảnh có thể sẽ không tương thích với các thư viện được dùng.